CMake helpers
-------------

Pybind11 can be used with ``add_subdirectory(extern/pybind23)``, or from an
install with ``find_package(pybind23 CONFIG)``. The interface provided in
either case is functionally identical.

.. cmake-module:: ../../tools/pybind23Config.cmake.in
